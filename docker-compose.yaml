services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: devodoro
      POSTGRES_PASSWORD: devodoro
      POSTGRES_DB: devodoro
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: